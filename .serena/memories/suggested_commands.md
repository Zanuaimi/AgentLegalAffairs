# Suggested Commands

Run from `AgentLegalAffairs/Code` unless noted.

- `npm install` — restore dependencies from `package-lock.json`.
- `npm run dev` — start Vite dev server.
- `npm run build` — production build/type-free validation currently available.
- `npm run preview` — preview the production build locally.

No configured `npm test`, lint, or format commands at this time.