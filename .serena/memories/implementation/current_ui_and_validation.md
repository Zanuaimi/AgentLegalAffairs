Current completed UI: requester role hides ContractChecklist and checklist progress; PDF modal omits checklist and shows only PDF + AI suggestions; AI queue debug card removed. AdminUsers uses Supabase Realtime Presence from App to show live Active/Inactive. LegalReviewers receives activeUserIds and displays same live statuses. Header displays `Role: <currentUser.role>` immediately before Profile. Required validation: run `npm run build` in Code after frontend code changes; when Supabase backend changes run local `supabase db reset` (never remote).