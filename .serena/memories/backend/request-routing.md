Request routing implemented in `supabase/migrations/202607140002_add_request_routing_functions.sql`: `auto_assign_legal_reviewer` least-load assigns a reviewer on requester submission; `assign_reviewer_as_manager` verifies manager role and same-department reviewer, records manager action; `route_request_as_reviewer` permits only assigned legal reviewer to return to requester or send to Legal Manager and requires a comment. Frontend uses `ReviewerRoutingPanel.jsx`, updated `ManagerActions.jsx`, backend service RPC wrappers, and App handlers. Queue E2E uncovered service_role privilege failure; `202607140001_grant_queue_worker_privileges.sql` fixes it and local Gemini test passed. Local reset run after these migrations; frontend build passed. A final single permitted Playwright workflow verified auto-assignment, but was blocked testing reviewer routing because sidebar navigation did not leave Dashboard when clicking Legal Requests; no source-code root cause was identified and no speculative fix was applied. No more E2E reruns were performed due to user-requested max 3 attempts.