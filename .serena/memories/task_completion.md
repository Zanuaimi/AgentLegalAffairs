# Task Completion

- After code changes in the React app, run from `AgentLegalAffairs/Code`:
  - `npm run build`
- If dependencies changed, run `npm install` first and ensure `package-lock.json` is intentionally updated.
- No automated tests/lint/format commands are configured yet; do not claim those validations passed unless new tooling is added and run.
- For memory maintenance sanity checks, user/agent can run `serena memories check` from the project root.