export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

/*
BEGINNER DOCUMENTATION:

1. What is PostCSS?
PostCSS is a tool that processes CSS before the browser receives it.

2. Why do we need this file?
Tailwind uses PostCSS to generate the final CSS styles for the app.

3. What is autoprefixer?
Autoprefixer adds browser-specific CSS prefixes when needed, improving browser compatibility.
*/
